import React from 'react'
import type { Meta } from '@types/routes'

const ForgotPassword = <T extends Meta>(props: T): JSX.Element => {
    return (
        <div>
            <h1>Forgot Password</h1>
            {/* Add your forgot password form here */}
        </div>
    )
}

export default ForgotPassword

import React from 'react'
import type { Meta } from '@/@types/routes'

const SignIn = <T extends Meta>(props: T): JSX.Element => {
    return (
        <div>
            <h1>Sign In</h1>
            {/* Add your sign in form here */}
        </div>
    )
}

export default SignIn 
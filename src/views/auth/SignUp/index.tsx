import React from 'react'
import type { Meta } from '@types/routes'

const SignUp = <T extends Meta>(props: T): JSX.Element => {
    return (
        <div>
            <h1>Sign Up</h1>
            {/* Add your sign up form here */}
        </div>
    )
}

export default SignUp 